import types

const
    KeyEvents* = {

        # Number Keys
        Key1, Key2, Key3,
        Key4, Key5, Key6,
        Key7, Key8, Key9,

        # Letter Keys
        KeyA, KeyB, KeyC, KeyD, KeyE, KeyF,
        KeyG, KeyH, KeyI, KeyJ, KeyK, KeyL,
        KeyM, KeyN, KeyO, KeyP, KeyQ, KeyR,
        KeyS, KeyT, KeyU, KeyV, KeyW, KeyX,
        KeyY, KeyZ,

        # Other Keys
        KeySpace, KeyEscape, KeySlash,
        KeyTab, KeyBackspace, KeyEnter

    }
